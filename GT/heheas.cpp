#include <iostream>
#include <bit

int main()
{
    short n;
    std::cin >> n;
    short i = 0;
    while (i < n)
    {
        std::string s;
        std::getline(std::cin, s);
    }

    return 0;
}